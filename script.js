const { createApp } = Vue;

createApp({
    data() {
        return {
            message: 'League of legends',
            // imageSource: "league-of-legends-cover1.webp"
        };
    }

    
  

}).mount('#app');
 